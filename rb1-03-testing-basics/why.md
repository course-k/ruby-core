# なぜこう書くか（rb1-03-testing-basics）

模範解説（`commentary.md`）を開く**前**に §1〜§3 を書く。§4 は読んだ後に書く。
`bin/check` が見るのは「この雛形と差分があること」だけで、中身に合否は付かない。

書き終えた後、Claude Code の入力欄で `/why-review rb1-03-testing-basics` と打つと、
一次情報と突き合わせた照合と、§3 への回答が返る（端末のコマンドではない。README の手順 5.5）。
任意の工程で、合否には効かない。

## 1. 書き写して気づいたこと
classにイニシャライザがない
変数の宣言は型もないしletやconstなどもない。pythonと同じか

refute_matchのときは関数呼び出しにカッコを使っている
正規表現と思われるが中身がわからん
no始まり？iってなんだっけ,はっとは始まりだよね
<!-- ここに書く -->

## 2. 次の問いに自分の言葉で答える

- テストメソッドの名前が `test_` で始まらないと何が起きるか。
想像でしかないが、多分テスト対象にならない。
- `assert_equal` の第 1 引数と第 2 引数は、どちらが期待値か。順番を入れ替えると失敗表示はどう変わるか。
左が期待値。入れ替えると失敗表示のexpect,actualが入れ替わるのでは
- `assert_raises` はブロックの中で何が起きることを確かめているか。
これは分からなかったことにも書いたが、なにが起きているかわからない

## 3. 分からなかったこと

1 行 1 問。問いの形で書く。§1 に「〜だろうか」と書いた疑問はここへ移す
（移し替えること自体が整理になる）。JS ならこう書くのにと思った箇所も、
「Ruby ではなぜそうしないのか」の形にすればここに入る。

課題の範囲を先取りする問いでも構わない。先送りはしない。

- Q1:メソッド名の最後に？をつけるとbooleanを返す規約みたいなのがあるときいたが、今回の課題は文字列返している。どういう意図があるのか
- Q2: class名の後ろの<は継承ってことかな
- Q3: classが空なのは型というか分類のためだけのクラスってことかな。いまいちこれのメリットがわかってない
- Q4: クラス名の後ろの::はなんだろう。Javaでも同じような記述を見た。何だっけ。クラスの中の呼び出すメソッドが自明なときに省略してかける、みたいなやつだっけ、Javaだと。
- Q:5 respond_toに設定していたのは課題2だと文字列だった気がするが、ここでは:をつかっている。
- Q:6 doが急に来ている。errorをraiseしている。何の処理か想像もつかない
- Q:7 skipという処理があるらしい、これも何の処理かわからない
- Q:8 not_set_yetはどこにも定義されてない
- Q:
- Q:


## 4. 突き合わせで変わったこと

`commentary.md` を読んだ**後**に書く。§1〜§3 のどこが、どう変わったか。
変わらなかったなら「変わらなかった」と書く。§1〜§3 は書き換えない——自力で到達した理解と
模範解説で到達した理解を分けて残すための欄分けなので、混ぜると照合が意味を失う。

<!-- ここに書く -->
requireはimportと同等だと思っていたが、機能登録もするのか？
予約が行われると書いている

Minitest::Testがテスト実行のために継承しておくクラスなのはわかったが、::の意義はわからない
上記の質問の答えがここに書いてある

setupが誰が読んでいるかわからない、だからいつnewした値がmemeに入っているかわからなかったが、テスト実行時に呼ばれるのか。
テスト開始前の初期設定みたいなものか

assertの逆がrefute
jsのテストだとなんだったかな、refuteではなかった気がする
nilを期待する時は別のもの
JSもそんなのがあった気がする
raisesでぶろっくをうけとるというのがピンとこない
