# frozen_string_literal: true

source "http://rubygems.org"

gem "debug", "1.11.1"
gem "irb", "1.16.0"
gem "minitest", "6.0.0"

# エディタ（Ruby LSP）が書式整形に使う。判定と同じ版に固定する（教材リポ Gemfile と同一）。
gem "rubocop", "1.91.0"
# rubocop が依存で json 3.x を引き込むと Ruby 4.0.6 同梱の json 2.18.0 と JSON.parse の引数仕様が変わる。
gem "json", "2.18.0"
