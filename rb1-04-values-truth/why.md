# なぜこう書くか（rb1-04-values-truth）

模範解説（`commentary.md`）を開く**前**に §1〜§3 を書く。§4 は読んだ後に書く。
`bin/check` が見るのは「この雛形と差分があること」だけで、中身に合否は付かない。

書き終えた後、Claude Code の入力欄で `/why-review rb1-04-values-truth` と打つと、
一次情報と突き合わせた照合と、§3 への回答が返る（端末のコマンドではない。README の手順 5.5）。
任意の工程で、合否には効かない。

## 1. 書き写して気づいたこと

自由記述。形は問わない。
先にコメントにあるドキュメントを読んだのでかなり理解度がちがう。
biggerが x<yをそのまま返さず、true,falseを返しているのはあえてかな？
普通はx < yだけでいいはず
case式でthenを書いてもいいし、かかなくてもいいということか
falsyなのはnil、falseだけ、潔い
言語によってことなるのはややこしいが
js, python同士もちがう。本来はrubyがあるべき姿なのかも。
値自体にちゃんと意味を見出すと言うか

## 2. 次の問いに自分の言葉で答える

- `if` 式を変数に代入できるのはなぜか。JS の `if` 文との違いは何か。
- Ruby で偽になる値は何か。`0` と `""` はどうなるか。
- `===` は何に使う演算子か。`==` と同じだと思って書くとどこで困るか。

## 3. 分からなかったこと

1 行 1 問。問いの形で書く。§1 に「〜だろうか」と書いた疑問はここへ移す
（移し替えること自体が整理になる）。JS ならこう書くのにと思った箇所も、
「Ruby ではなぜそうしないのか」の形にすればここに入る。

課題の範囲を先取りする問いでも構わない。先送りはしない。

- Q1: rejectは定義済？
- Q2: reject内にはブロックが定義されている。コールバック関数のようなものか。JSでいう配列のfilterの逆か。falseなやつだけが返ってきて、新しい配列になる。rejectだからfalseなやつだけかえってくるのでは。|value|が配列の要素を受け取る
- Q2:
- Q2:
- Q2:
- Q2:
- Q2:
- Q2:
- Q2:

## 4. 突き合わせで変わったこと

`commentary.md` を読んだ**後**に書く。§1〜§3 のどこが、どう変わったか。
変わらなかったなら「変わらなかった」と書く。§1〜§3 は書き換えない——自力で到達した理解と
模範解説で到達した理解を分けて残すための欄分けなので、混ぜると照合が意味を失う。

<!-- ここに書く -->
print_hello_worldはputsの戻り値を見せたかったのか
js,pythonも戻り値はundifined系ではなかったかな？
case で正規表現が使えるのはありがたい
他の言語ではない？JSだとREGEXとか使うよね？

===と==の違いってなんなんだ？
JSだとちがう、pythonだとそもそも===がないか？
===は糖衣構文的な？書き換えられるもの？==は書き換えられない？

ifが値を返すのが一番の衝撃かもね。
jsだと式と文があって、変数に代入できるのは式
rubyだと文も代入できる。値を返すから
